---
description: Pick the top ready task, restate it, and start test-first
---
1. Read docs/BACKLOG.md. Take the item in Now. If Now is empty, promote the top item of Next — only if it has AC and size ≤ M; otherwise stop and tell me exactly what it's missing.
2. Restate task + AC in two lines. If docs/plans/<ID>.md exists, resume from its Parked state; otherwise create it from docs/plans/_TEMPLATE.md.
3. Begin with the first failing test from the plan.

One task only — never pull additional items into the same session.
